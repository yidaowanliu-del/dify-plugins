[project]
name = "aosom-lark-message"
version = "0.0.2"
description = "Send Lark/Feishu messages to users and group chats"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dify-plugin>=0.9.0",
    "httpx>=0.28.1",
]
